# Task Manager

Role-based task management app with admin dashboard and user activity tracking.

## Stack

- Backend: Node.js, Express, MongoDB (Mongoose), JWT
- Frontend: React (Vite), React Router, Context API, Tailwind CSS

## Structure

- `/backend` — REST API
- `/frontend` — React client

## Setup

See `/backend` and `/frontend` for individual setup instructions.
